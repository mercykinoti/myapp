<% provide(:title, "Home") %>

	<div class="container-fluid">
	 <div class="col-md-12 well home">
	 	
	 	<div class="blue">
	 		<p>This is the life</p>
	 	</div>
	 </div>
	 <div class="col-md-12 projects">
	
	  <h1 class="text-center">Articles</h1>
		 
	   <div class="row">
	   	<% @articles.each do |article| %>
		<div class="col-md-4">
			<h3 class="text-center"><%= article.title %></h3>
			<p><%= article.text %></p> 
		</div>
		<% end %>
	</div>
	</div>
	</div>
